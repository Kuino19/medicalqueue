{
  "name": "mediq",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd src && npm run dev",
    "build": "cd src && npm install && npm run build",
    "start": "cd src && npm run start",
    "lint": "eslint"
  },
  "dependencies": {},
  "devDependencies": {}
}

